#include<stdio.h>
int lcm(int,int);
int main()
{
    int a,b,c;
    printf("enter two numbers to find LCM :");
    scanf("%d%d",&a,&b);
    c=lcm(a,b);
    printf("LCM of %d and %d is %d",a,b,c);
}
int lcm(int x,int y)
{

}